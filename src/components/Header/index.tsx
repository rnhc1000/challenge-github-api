import './styles.css';

export default function Header() {

    return (

        <header className="header-container">
            <div className="text-header">
                <p>GitHub API</p>
            </div>
        </header>
    );
}